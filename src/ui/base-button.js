import styled from 'styled-components';

export const BaseButton = styled.button`
  padding: 8px 16px;
  font-family: inherit;
  font-size: 16px;
  color: inherit;
  border: none;
  border-radius: 2px;
  background: none;
  appearance: none;
  cursor: pointer;
  user-select: none;
`;
